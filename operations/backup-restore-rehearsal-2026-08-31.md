# DB 백업·복구 리허설 결과 — 2026-08-31

## 검증 범위

- MySQL 논리 백업 생성
- AES-256-CBC/PBKDF2 암호화 및 SHA-256 체크섬 생성
- 운영 DB와 분리된 임시 MySQL 8.0 컨테이너 복구
- 복구된 스키마와 `toilet` 데이터 존재 여부 확인

## 실행 결과

- 백업 생성: 성공 (`toilet-db-20260831-120517.sql.gz.enc`)
- 체크섬 검증: 성공
- 임시 복구: 성공(MySQL 8.0 임시 컨테이너)
- 스키마/데이터 검증: 테이블 11개, 화장실 53,576건 확인
- 운영 DB 영향: 없음(임시 컨테이너만 사용)

## 보완 이력

- 최소권한 DB 계정에서 tablespace 조회 권한이 없어 `--no-tablespaces`를 적용했다.
- 대용량 확장 INSERT 복구 중 연결이 끊기는 문제를 확인해 `--skip-extended-insert`와 복구 컨테이너 packet 상한을 적용했다.
- 최종 스크립트로 다시 백업한 결과 체크섬·전체 복구·데이터 검증이 모두 통과했다.
- 매일 03:15 KST 실행되는 `geupddong-mysql-backup.timer`를 활성화했으며, 재부팅 및 예약 시각 누락 시에도 실행되도록 `Persistent=true`를 적용했다.
